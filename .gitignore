# Environment files (.env, .env.prod)
.env
.env.*
!.env.example
!.env.prod.example

# Runtime state written by the containers themselves
zitadel/.output/

# OS
Thumbs.db
.DS_Store

# Edge runtime state (Let's Encrypt account, certificates, ACME challenges)
edge/certbot/
